
const initialState = {
    tokens: [],
    // Other token related state
  };
  
  const tokenReducer = (state = initialState, action) => {
    switch (action.type) {
      // Handle different actions
      default:
        return state;
    }
  };
  
  export default tokenReducer;
  